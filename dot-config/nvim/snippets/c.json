{
  "main": {
    "prefix": "main",
    "body": [
      "int main(int argc, char *argv[]) {",
      "    $0",
      "    return 0;",
      "}"
    ],
    "description": "WTFFFMain function"
  },
  "printf": {
    "prefix": "printf",
    "body": [
      "printf(\"$1\\n\"$2);$0"
    ],
    "description": "printf statement"
  },
  "for": {
    "prefix": "for",
    "body": [
      "for (int ${1:i} = 0; $1 < ${2:N}; $1++) {",
      "    $0",
      "}"
    ],
    "description": "For loop"
  },
  "if": {
    "prefix": "if",
    "body": [
      "if (${1:condition}) {",
      "    $0",
      "}"
    ],
    "description": "If statement"
  },
  "ifelse": {
    "prefix": "ifelse",
    "body": [
      "if (${1:condition}) {",
      "    $2",
      "} else {",
      "    $0",
      "}"
    ],
    "description": "If-Else statement"
  },
  "header_guard": {
    "prefix": "guard",
    "body": [
      "#ifndef ${1:HEADER}_H",
      "#define $1_H",
      "",
      "$0",
      "",
      "#endif // $1_H"
    ],
    "description": "Header guard"
  },
  "function": {
    "prefix": "func",
    "body": [
      "${1:void} ${2:function_name}(${3:void}) {",
      "    $0",
      "}"
    ],
    "description": "Function definition"
  }
}
